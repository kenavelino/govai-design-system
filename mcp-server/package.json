{
  "name": "govai-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for the GovAI Design System — exposes Design.md, tokens, global styles, and components from GitHub for AI coding tools.",
  "private": true,
  "type": "module",
  "bin": {
    "govai-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
